name: GitHub Actions Demo
run-name: ---- github.actor ${{ github.actor }} ---- 
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo " 变量：{{ github.event_name }} ${{ github.event_name }} "
      - run: echo " 变量：{{ runner.os }} ${{ runner.os }} "
      - run: echo " 变量：{{ github.ref  }} ${{ github.ref }}  变量： {{github.repository}}  ${{ github.repository }}"
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo " 变量：{{ github.repository }} ${{ github.repository }} "
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }} #github.workspace 
      - run: echo " 变量： {{job.status}} ${{ job.status }}"
