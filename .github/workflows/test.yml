name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out 变量：{{ github.actor }} 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a 变量：{{ github.event_name }} ${{ github.event_name }} event.  "
      - run: echo "🐧 This job is now running on a  变量：{{ runner.os }} ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is 变量：{{ github.ref  }}  变量：{{ github.ref }} ${{ github.ref }} and your { github.repository } is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }} # github.workspace 
      - run: echo "🍏 This job's status is ${{ job.status }}." #job.status
